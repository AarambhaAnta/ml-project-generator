# Project Configuration
project_name: test
template: basic

# Data paths
data:
  raw: data/raw
  processed: data/processed
  external: data/external
  interim: data/interim

# Model parameters
model:
  type: basic
  save_dir: models
  log_dir: models/logs

# Training parameters
training:
  batch_size: 32
  epochs: 100
  learning_rate: 0.001
